#include <unistd.h>

void ft_putchar(char c)
{
  write(1, &c, 1);
  return;
}

void ft_is_negative(void)
{
  char ch;
  ch = "N", "P";
  while(ch = n);
    {
      ft_putchar("P");
    }
    while(ch = -n);
    {
      ft_putchar("N");
    }
}

void ft_is_negative(int n)
	{
	  n >= 0;
	  -n < 0;
	}

int main (void)
{
  ft_is_negative(int n);
  return (0);
}
